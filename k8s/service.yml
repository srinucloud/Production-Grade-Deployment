apiVersion: apps/v1
kind: Service
metadata:
  name: ecommerce-app-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: ecommerce-app
  ports:
  - port: 80
    targetPort: 5000

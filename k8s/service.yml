apiVersion: apps/v1
kind: Service
metadata:
  name: ecommerce-app-service
  namespace: ecommerce-app
spec:
  type: LoadBalancer
  selector:
    app: ecommerce-app
  ports:
  - port: 80
    targetPort: 5000
